query launch_detail($launchId: ID!) {
    launch(id: $launchId) {
        mission_name
        launch_date_utc
        details
        rocket {
            rocket_name
            rocket_type
        }
        links {
            article_link
            video_link
            flickr_images
        }
    }
}
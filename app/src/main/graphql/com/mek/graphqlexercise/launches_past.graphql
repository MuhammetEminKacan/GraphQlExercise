query launches_past($limit: Int){
    launchesPast(limit: $limit) {
        id
        mission_name
        launch_date_utc
        links {
            flickr_images
            mission_patch
            mission_patch_small
        }
    }
}
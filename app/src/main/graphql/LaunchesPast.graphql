query LaunchesPast {
    launchesPast(
        limit: 50,
        sort: "launch_date_utc",
        order: "desc"
    ) {
        id
        mission_name
        launch_date_utc
        links {
            mission_patch_small
            mission_patch
            flickr_images
        }
    }
}